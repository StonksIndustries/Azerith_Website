{
  "name": "azerith_website",
  "version": "0.1.0",
  "description": "Lands of Azerith Website",
  "scripts": {
    "dev": "parcel src/index.html",
    "clean": "rm -rf .parcel-cache dist",
    "build": "npm run clean && parcel build src/index.html --public-url ./",
    "svgo": "find src/svg2 -type f -name '*.svg' -exec svgo --input={} --output={} \\;"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0",
    "prettier": "^3.3.1"
  },
  "devDependencies": {
    "sharp": "^0.31.3",
    "svgo": "^3.3.2"
  }
}
